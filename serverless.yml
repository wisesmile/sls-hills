service: sls-hills
app: sls-hills
org: wisesmile

custom:
  bucket: wiesemile-test

provider:
  name: aws
  runtime: nodejs12.x
  iamRoleStatements:
    - Effect: Allow
      Action:
        - s3:PutObject
        - s3:PutObjectAcl
      Resource: "arn:aws:s3:::${self:custom.bucket}/*"

plugins:
  # - serverless-jest-plugin

functions:
  jokes:
    handler: functions/jokes/handler.handler
    events:
      - http:
          path: /jokes
          method: post
          cors: true
  jakes:
    handler: functions/jokes/handler.handler
    events:
      - http:
          path: /jakes
          method: post
          cors: true
  save:
    handler: functions/write-to-s3/handler.copy.save
    events:
      - http:
          path: /file-to-s3
          method: post
          cors: true
    environment:
      BUCKET: ${self:custom.bucket}
